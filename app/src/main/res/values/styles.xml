<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Volunteer" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/vPrimaryColor</item>
        <item name="colorPrimaryDark">@color/vPrimaryDarkColor</item>
        <item name="colorAccent">@color/vSecondaryColor</item>
    </style>

    <style name="VolunteerHome" parent="Volunteer">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="HospitalDashboard" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/hPrimaryColor</item>
        <item name="colorPrimaryDark">@color/hPrimaryDarkColor</item>
        <item name="colorAccent">@color/hSecondaryColor</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
